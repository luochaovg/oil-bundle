bsw.configure({
    data: {},
    method: {
        chatTabsSwitch(key) {
            this.redirect(this.getBswData($(`#${key}`)));
        },
    },
    logic: {
        chartInit(v) {
            {% for item in chart.items %}
              bsw.chart({
                  id: '{{ item.name }}',
                  theme: '{{ item.theme }}',
                  option: {{ item.optionStringify|raw }}
              });
            {% endfor %}
        }
    }
});