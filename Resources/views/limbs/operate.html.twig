{% set uuid = scaffold.uuid %}

<div class="app-operate {{ operate.position }}" v-show="{{ operate.buttons ? 'true' : 'false' }}">
  {% for form in operate.buttons %}
    {% include '@LeonBsw/form/button.html.twig' %}
  {% endfor %}
</div>
