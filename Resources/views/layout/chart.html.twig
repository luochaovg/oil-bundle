{% extends 'layout/blank.html.twig' %}
{% set tplContentClass = ['bsw-chart-container', tplContentClassDiy ?? null]|filter(v => v)|join(' ') %}

{% block page %}
  {% block filter_before %}{% endblock %}
  {% include 'limbs/filter.html.twig' %}
  {% block filter_after %}{% endblock %}
  {% if chart.menu is not empty %}
    <a-tabs
      class="bsw-chart-tabs"
      default-active-key="{{ scaffold.route }}"
      @change="chat_tabs_switch"
      :tab-bar-gutter="6"
      type="card"
    >
      {% for index, link in chart.menu %}
        <a-tab-pane key="{{ link.route }}">
          <span
            id="{{ link.route }}"
            bsw-data='{"location": "{{ link.url }}"}'
            slot="tab">
            {{ link.icon|icon|raw }}{{ link.label|trans({}, 'twig') }}
          </span>
        </a-tab-pane>
      {% endfor %}
    </a-tabs>
  {% endif %}
  <a-divider></a-divider>
  {% block chart_before %}{% endblock %}
  <div class="bsw-chart-list">
    {% for item in chart.items %}
      {% include 'limbs/chart.html.twig' with {chart: item} %}
    {% endfor %}
  </div>
  {% block chart_after %}{% endblock %}
{% endblock %}

{% block javascript %}
{% include 'javascript/filter.md.twig' %}
{% include 'javascript/chart.md.twig' %}
{% endblock %}