<a-input-number
  v-if="{{ form.needId ? 'true' : 'false' }}"
  {% if form.class %}
    class="{{ form.class }}"
  {% endif %}
  name="{{ form.name }}"
  :style="{{ form.style }}"
  :disabled="{{ form.disabled ? 'true' : 'false' }}"
  size="{{ form.size }}"
  :min="{{ form.min }}"
  :max="{{ form.max }}"
  step="{{ form.step }}"
  placeholder="{{ form.placeholder }}"
  v-decorator="['{{ form.key }}', { initialValue: `{{ form.value }}`, rules: {{ form.formRules }} }]"
></a-input-number>

<a-upload
  {% if form.class %}
    class="{{ form.class }}"
  {% endif %}
  :style="{{ form.style }}"
  :disabled="{{ form.disabled ? 'true' : 'false' }}"
  accept="{{ form.accept }}"
  action="{{ form.url }}"
  list-type="{{ form.listType }}"
  :show-upload-list="false"
  :file-list="{{ form.fileListKey }}"
  name="{{ form.flag }}"
  :data="{ file_flag: '{{ form.flag }}' }"
  {% if form.varNameForKey is not empty %}
    @click.native="{{ form.varNameForKey }} = '{{ form.key }}'"
  {% endif %}
  :data-bsw.prop="{{ form.formData }}"
  @change="{{ form.change }}"
  {{ form.attributes|raw }}
  v-decorator="['{{ form.key }}FileList']"
>
  {% if form.needTips and form.varNameForTips is not empty %}
    <a-popover>
      <template slot="content">
        <a-table
          :columns="{{ form.varNameForTips }}.{{ form.key }}.columns"
          :data-source="{{ form.varNameForTips }}.{{ form.key }}.list"
          :pagination="false"
          :scroll="{ x: 340 }"
          bordered
          size="small"
        ></a-table>
      </template>
      <a-button
        type="{{ form.buttonType }}"
        :style="{{ form.buttonStyle }}"
      >
        {{ scaffold.cnf.iconUpload|icon|raw }}
        {{ form.buttonLabel|trans({}, 'twig') }}
      </a-button>
    </a-popover>
  {% else %}
    <a-button
      type="{{ form.buttonType }}"
      :style="{{ form.buttonStyle }}"
    >
      {{ scaffold.cnf.iconUpload|icon|raw }}
      {{ form.buttonLabel|trans({}, 'twig') }}
    </a-button>
  {% endif %}
</a-upload>
